[project]
name = "purplesol-backend"
version = "1.0.0"
description = "AI Retail Intelligence Platform - Backend"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
